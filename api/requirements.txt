# Core Flask dependencies for Vercel
Flask==3.0.0
Werkzeug==3.0.1
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0

# HTTP and API
requests==2.31.0

# Note: Heavy ML dependencies removed for Vercel compatibility
# TensorFlow, NumPy, Matplotlib, etc. are too large for Vercel free tier
# For full functionality, use local deployment
